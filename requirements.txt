# This requirements file lists all third-party dependencies for this project.
#
# Run 'pip install -r requirements.txt -t lib' to install these dependencies
# in `lib/` subdirectory.
#
# Note: The `lib` directory is added to `sys.path` by `appengine_config.py`.
Flask==0.10
requests==2.6.0 # Not really used in this example, but this is the only version of requests that works with Flask unfortunately
peewee==2.6.1
pygments==2.0.2
markdown==2.6.2
micawber==0.3.2
BeautifulSoup==3.2.1